# Core App Requirements
streamlit>=1.32.0
numpy>=1.21.0,<1.24.0

# Agno AI Core and Tools
agno
duckduckgo-search

# Multilingual Support
langdetect                 # Detects the language of input text
googletrans==4.0.0-rc1     # Google Translate API wrapper (for translation)
transformers>=4.25.0       # For multilingual transformer models (e.g., mBERT, XLM-R)
sentencepiece              # Required for tokenization of some multilingual models
torch>=1.13.0              # Backend for transformers (ensure it's compatible with your setup)

# Optional NLP utilities
nltk
spacy
